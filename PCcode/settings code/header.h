#include <vector>
#include <string>
#include <algorithm>
#ifndef header_H
#define header_H
std::vector<std::string> splitStringByPlus(const std::string& input);

bool isNumber(const std::string& str);

#endif